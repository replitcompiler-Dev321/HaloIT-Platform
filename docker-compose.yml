version: '3.8'
services:
  halo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: halo-system
    ports:
      - "3000:3000"
    volumes:
      - ./halo-system/backend:/app
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
