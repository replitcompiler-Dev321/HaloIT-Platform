version: '3.8'
services:
  halo:
    build:
      context: ./halo-system
    container_name: halo-system
    ports:
      - "3000:3000"
    volumes:
      - ./halo-system:/app
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
